enum SeatStatus {
    AVAILABLE = 0,
    RESERVED = 1,
    IN_USE = 2,
    MAINTENANCE = 3
}

enum OrderStatus {
    PENDING = 0,
    CONFIRMED = 1,
    CANCELLED = 2
}

enum UserType {
    MEMBER = 0,
    OWNER = 1,
    ADMIN = 2
}